#!/bin/gawk
$1 != "" && $1 !~ /^EX_/ {   
    gsub (":", "", $1);
    if ($2 > 0) {
        print "R_" $1, $2 * 1.0, 0.0;
    }
    else if  ($2 < 0) {
        print "R_" $1 "_rev", $2 * -1.0, 0.0;
    }
}
